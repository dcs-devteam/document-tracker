= form_for office_staff do |f|
  = render partial: "partials/form_errors", locals: { object: office_staff }

  .field
    = f.label :name
    = f.text_field :name

  .field.actions
    = f.submit "Create Office Staff", class: "button blue"